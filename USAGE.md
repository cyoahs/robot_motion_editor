# 快速使用指南

## 已修复的问题

### 1. URDF 文件加载
✅ 已安装并正确配置 `urdf-loader@0.12.3`
✅ 支持完整的 URDF 解析，包括：
  - 关节（joints）解析
  - 链接（links）和视觉模型（visual）
  - Mesh 文件加载（STL、DAE等）
  - 包路径解析（package://）

### 2. 关键帧管理
✅ 添加关键帧功能
✅ 删除关键帧功能（两种方式）：
  - 点击"删除当前关键帧"按钮
  - 右键点击时间轴上的关键帧标记
✅ 关键帧在时间轴上显示为绿色标记
✅ 鼠标悬停时标记会放大并变色

## 使用步骤

1. **启动服务器**
   ```bash
   nvm use
   npm ci
   npm run dev
   ```
   或
   ```bash
   ./run.sh
   ```

2. **打开浏览器**
   访问终端中 Vite 输出的地址（项目默认 http://localhost:3000）

3. **加载机器人模型**
   - 打开“添加机器人”，可直接选择 `Unitree > G1/H2`
   - 也可选择“上传 URDF”，再选择包含 URDF 和 mesh 的完整文件夹
   - “本地处理，数据安全”设置中的“优化上传 Mesh”默认开启，会在浏览器本地降低过高的面数；需要保留原始网格时可关闭
   - 等待模型加载完成
   - 3D 视图中会显示机器人模型

4. **加载轨迹**
   - 点击“加载 CSV 轨迹”
   - 选择 unitree CSV 或 seed CSV 文件（或使用 example_trajectory.csv）
   - 时间轴会更新为轨迹长度；场景轨迹会自动保持相同帧数和 FPS

5. **编辑关键帧**
   - 拖动时间轴到想要编辑的时刻
   - 在右侧面板调整关节角度
   - 点击"添加关键帧"
   - 绿色标记会出现在时间轴上

6. **删除关键帧**
   - 方法1: 移动到关键帧位置，点击"删除当前关键帧"
   - 方法2: 右键点击时间轴上的绿色标记

7. **导出轨迹**
   - 从导出菜单选择“机器人编辑后轨迹”或“机器人原始轨迹”
   - 编辑轨迹导出会合并 `base + residual/Fix`；原始轨迹导出只包含只读 base
   - 机器人可选择 unitree 或 seed 格式；机器人与场景导出始终使用共享 FPS

## 场景轨迹

1. 点击“上传场景”，选择独立的场景 URDF 文件夹。
2. 场景与机器人共用唯一的帧数/FPS；加载机器人 CSV 或修改轨迹长度时会同步场景轨迹。
3. 切换编辑对象到“场景”，即可使用同一时间轴、关键帧和曲线编辑流程。
4. 勾选某个自由度旁的 `Fix` 后，该自由度在整条编辑轨迹和导出中都保持当前设定值；取消勾选恢复关键帧插值。
5. 场景没有独立的 CSV 导入按钮；机器人 CSV 是唯一导入时钟，机器人与场景仍通过各自按钮独立导出。

## 从零创建轨迹

1. 先加载机器人和/或场景 URDF，再切换到“创建模式”。
2. 设置唯一一组帧数与 FPS，点击“创建零轨迹”；已加载的机器人/场景会同时创建对齐轨迹。
3. 修改帧数或 FPS 后点击“应用长度”，两条轨迹会一起更新；扩展时复制末帧，裁剪时会移除范围外关键帧。
4. 创建模式只显示编辑后的单个视口，完成后可直接编辑并使用对应轨道的导出按钮。

## CSV 格式

加载后两种格式都会转换为统一的内部 SI 表示：位置使用米、关节使用弧度、基体姿态使用四元数。机器人支持两种格式互相转换导出；场景只导出 Unitree。

| 项目 | Unitree | Seed |
|---|---|---|
| 表头 | 无 | 固定 7 列表头 + 关节列名 |
| 基体平移 | m | cm |
| 基体旋转 | 四元数 `xyzw` | roll/pitch/yaw，degree，ZYX 组合 |
| 关节 | rad | degree |
| 默认导入 FPS | 50 | 120 |

### Unitree 格式

```csv
# x,y,z,qx,qy,qz,qw,joint_1,joint_2,joint_3
0,0,0.8,0,0,0,1,0.1,-0.2,0.3
0.01,0,0.8,0,0,0.043619387,0.999048222,0.12,-0.18,0.28
```

- 无表头，每一行代表一帧；示例第一行是可省略的注释，不是普通 CSV 表头。
- 第 1–3 列：基体位置 `x,y,z`，单位 m。
- 第 4–7 列：基体四元数 `qx,qy,qz,qw`；加载时会归一化，长度小于 `1e-6` 时使用单位四元数 `0,0,0,1`。
- 第 8 列起：关节角，单位 rad，顺序必须与 URDF 控制面板一致。
- 默认导入 FPS 为 50；文件本身不包含 FPS。

### Seed 格式

```csv
Frame,root_translateX,root_translateY,root_translateZ,root_rotateX,root_rotateY,root_rotateZ,joint_1_dof,joint_2_dof,joint_3_dof
0,0,0,80,0,0,0,5,-10,15
1,1,0,80,0,0,5,6,-9,14
```

- 有表头，前七列名称和顺序必须完全符合示例（匹配时不区分大小写，首尾空格会忽略）。
- `Frame`：必须是有限数值，但导入不按它排序或计时；当前按数据行顺序播放。导出时会重新生成从 0 开始的连续编号。
- `root_translateX/Y/Z`：基体位置，单位 cm，加载后转换为 m。
- `root_rotateX/Y/Z`：单位 degree，分别表示 roll/pitch/yaw，旋转组合为 `Rz(yaw) * Ry(pitch) * Rx(roll)`（ZYX）。
- 后续关节列：单位 degree，加载后转换为 rad。列名会在 Seed 往返导出时保留；从其他格式转换时生成 `joint_1_dof` 等名称，但数值仍按列位置对应 URDF 关节。
- 默认导入 FPS 为 120；文件本身不包含可靠的采样间隔。
- 四元数转换为欧拉角时可能出现另一组等价角度，接近万向节锁时尤其如此；再次加载后的空间姿态保持等价，不应比较欧拉角字面值。

### 通用校验和导出规则

- 加载 CSV 前必须先加载机器人 URDF；关节列数量必须等于 URDF 的可动关节数。
- 首个有效行匹配 Seed 固定表头时识别为 Seed，否则按 Unitree 解析；两种格式都至少需要 7 列。
- 空行和以 `#` 开头的整行注释会被忽略；不支持行尾注释、带引号字段或其他分隔符。Unitree 的普通文本表头会被当成错误数据。
- 每个数据行必须具有完全相同的列数，且每一列都必须是有限数值；缺列、多列、空值、`NaN` 或 `Infinity` 会拒绝整个文件，不会静默错位关节。
- 加载时会用格式默认值提示确认 FPS；确认后的 FPS 成为机器人/场景共享时钟。
- 机器人可按源格式导出，也可在 Unitree/Seed 之间转换。选择格式不会把共享 FPS 改成该格式的默认值，也不会改变机器人和场景的轨迹长度。
- 场景自由度可能是 prismatic 平移关节，而 Seed 会把所有后续自由度都按角度换算，因此场景轨迹仅允许 Unitree 导出。

## 关键帧残差系统

- **Base轨迹**：CSV 加载的原始数据（只读）
- **Residual残差**：编辑的关键帧相对于 base 的偏移
- **插值**：两个关键帧之间线性插值
- **最终输出**：`final = base + residual`

## 快捷操作

- **左键拖动**：旋转视角
- **右键拖动**：平移视角
- **滚轮**：缩放
- **点击关键帧标记**：跳转到该帧
- **右键关键帧标记**：删除关键帧

## 注意事项

1. 必须先加载 URDF，再加载 CSV
2. CSV 中的关节数量应与 URDF 中的可动关节数量匹配
3. 关键帧会自动保存残差值
4. 导出时会自动合并所有关键帧

## 故障排除

**问题：URDF 加载后看不到模型**
- 检查 mesh 文件是否在正确的相对路径
- 查看浏览器控制台是否有加载错误
- 确认 URDF 文件格式正确

**问题：关节控制不响应**
- 确保 URDF 已正确加载
- 检查关节限制范围
- 查看控制台是否有错误信息

**问题：导出的轨迹不正确**
- 确认已正确设置关键帧
- 检查关键帧是否在有效帧范围内
- 验证 base 轨迹数据正确
